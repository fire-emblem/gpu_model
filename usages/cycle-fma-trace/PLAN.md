# Plan

1. Resolve the repository root from the script location.
2. Verify that `build/fma_loop_cycle_trace_main` exists.
3. Run the example with a representative cycle-mode configuration.
4. Save stdout into `results/stdout.txt`.
5. Let the example write trace and timeline files directly into `results/`.
