#pragma once

// PHASE2-DELETE(runtime-program): legacy public header kept only for deletion order.
#include "gpu_model/program/executable_kernel.h"

namespace gpu_model {

using KernelProgram = ExecutableKernel;

}  // namespace gpu_model
