# Doc

Command executed by this usage bundle:

```bash
./build/vecadd_main
```

Expected artifact:

- `results/stdout.txt`

Expected behavior:

- prints the first 16 vector-add outputs,
- exits successfully,
- does not require any extra runtime arguments.
