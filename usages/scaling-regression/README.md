# Scaling Regression Usage

This usage bundle runs the parameterized scale coverage that exercises requested functional and cycle launch sizes.

Use it when you want:

- a regression sweep over representative launch scales,
- a captured transcript of the long-running scale tests,
- a reproducible stress command for CI or manual validation.
