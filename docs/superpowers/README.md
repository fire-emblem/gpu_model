# `docs/superpowers`

这里存放围绕当前主线重构的实施文档。

目录说明：

- `specs/`: 当轮重构包的目标边界、命名约束、完成条件
- `plans/`: 对应的执行计划和阶段拆分

使用约定：

- 同名 `spec` 与 `plan` 共同描述一个重构包
- 若 `plan` 中的任务已完成，以当前代码和最近提交为准
- 历史归档计划仍保留原始任务表述，不要求逐字反映最终代码命名
