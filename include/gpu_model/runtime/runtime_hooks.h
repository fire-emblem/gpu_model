#pragma once

// PHASE2-DELETE(runtime-program): legacy public header kept only for deletion order.
#include "gpu_model/runtime/hip_runtime.h"

namespace gpu_model {

using RuntimeHooks = HipRuntime;

}  // namespace gpu_model
