# Functional VecAdd Usage

This usage bundle runs the compiled `vecadd_main` example in functional mode and captures its stdout.

Use it when you want:

- a minimal sanity check for the build,
- a readable example of global-memory result output,
- a reference output file that can be diffed later.
