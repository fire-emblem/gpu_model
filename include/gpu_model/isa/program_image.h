#pragma once

// PHASE2-DELETE(runtime-program): legacy public header kept only for deletion order.
#include "gpu_model/program/program_object.h"

namespace gpu_model {

using ProgramImage = ProgramObject;

}  // namespace gpu_model
