#pragma once

// Bridge header — canonical location is gpu_arch/register/register_file.h
// Register file runtime state is defined in gpu_arch/register/ for now.
// This file provides backward compatibility.
#include "gpu_model/gpu_arch/register/register_file.h"
