# Plan

1. Resolve the repository root from the script location.
2. Verify that `build/tests/gpu_model_tests` exists.
3. Run only the scale-focused GoogleTest filters.
4. Save the full transcript into `results/stdout.txt`.
