#pragma once

// PHASE2-DELETE(runtime-program): legacy public header kept only for deletion order.
#include "gpu_model/runtime/model_runtime.h"

namespace gpu_model {

using ModelRuntimeApi = ModelRuntime;

}  // namespace gpu_model
