# 04 Raw Code Object Decode

这个例子展示：

- 编译一个最小 HIP 可执行物
- 用项目内 decode / formatter 工具把 code object 指令打印出来
- 验证输出包含 `fmt`、`op_type`、`class` 等字段

运行：

```bash
./workflows/raw-code-object-decode/run.sh
```
