# Cycle FMA Trace Usage

This usage bundle runs the configurable cycle-mode FMA loop example and captures:

- stdout summary,
- text trace,
- JSONL trace,
- ASCII timeline.

Use it when you want a concrete cycle-level trace artifact for manual inspection.
